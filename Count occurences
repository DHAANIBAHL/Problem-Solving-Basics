myList = [1,3,2,4,1,3,2,2,3,1,1,4]
setList = set(myList)

for item in setList:
    count = 0
    for value in myList:
        if value == item:
            count += 1
    print(f"{item} occurs {count} times.")
